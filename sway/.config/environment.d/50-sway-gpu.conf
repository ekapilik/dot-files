# Force wlroots to use only the Intel GPU (card1).
# NVIDIA (card2) has no display connectors on this machine — including it
# causes DRM atomic commit failures and blank outputs.
WLR_DRM_DEVICES=/dev/dri/card1
